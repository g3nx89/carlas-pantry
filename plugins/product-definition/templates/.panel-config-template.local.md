---
# Panel Configuration — Generated by Panel Builder
# DO NOT manually edit YAML frontmatter — managed by workflow

version: 1
preset: "{PRESET_NAME}"  # preset name or "custom"
domain: "{DOMAIN_DESCRIPTION}"
created: "{ISO_TIMESTAMP}"
updated: "{ISO_TIMESTAMP}"

members:
  - id: "{MEMBER_ID}"
    role: "{ROLE}"
    perspective_name: "{PERSPECTIVE_NAME}"
    question_prefix: "{QUESTION_PREFIX}"
    weight: 0.00
    focus_areas:
      - "{FOCUS_AREA_1}"
      - "{FOCUS_AREA_2}"
    prd_section_targets:
      - "{PRD_SECTION_1}"
      - "{PRD_SECTION_2}"
    analysis_steps:
      step_1: "{STEP_1_DESCRIPTION}"
      step_2: "{STEP_2_DESCRIPTION}"
      step_3: "{STEP_3_DESCRIPTION}"
      step_4: "{STEP_4_DESCRIPTION}"
      step_5: "{STEP_5_DESCRIPTION}"
    domain_guidance: |
      {DOMAIN_GUIDANCE_BLOCK}

  # Repeat for each panel member (2-5 total)
  # Weights across all members MUST sum to 1.0
---

## Panel Rationale

{RATIONALE_TEXT}
